# Временные исключения и готовый отчёт — 22.09.2026

## Историческое правило до уточнения расчётов

Это исключение отменено после согласования недельных правил 22.09.2026.
Текущее поведение описано в [проверке недельного расчёта](glopro-weekly-2026-09-22.md).

До уточнения правил расчёта временно пропускались:

- ООО «НК АРТЭЛЬ», ID `78756`.
- «Китай», ID `78749`.

Настройка сохранена в `operators/glopro.md`, поле `excluded_clients`.
В автоматической выгрузке сравнивается точный ID; смена названия не отменяет
исключение, а другая фирма с тем же названием продолжает обрабатываться.
Исключение применяется до открытия договоров. Полнота списка остальных фирм
проверяется как прежде. В обработчике расчёта также проверяется идентичность
исходника; причины пропуска сохраняются в аудите и показываются в интерфейсе.
Коэффициенты остальных фирм не менялись.

## Готовый комплект

- Новый запуск: `e67a66cc68a643e7a4eb8fc04be8d51d`.
- Источник: проверенные XLSX запуска `97ce273393254d9b8146adc5b7a927ff`.
- Период: **18–21.09.2026**, дата активации: **22.09.2026**.
- Статус: **completed / Готово**.
- Обработано **30 фирм**, **12** с операциями для активации, **18** без операций.
- Временно исключены **2 фирмы**, ошибок расчёта **0**.
- Включены Markdown, TXT, сводный Excel, 30 исходников, инструкция и аудит.

Результаты расчётов всех 30 фирм полностью совпадают с ранее проверенными
результатами. Новый аудит содержит `reused_from_run` и контрольные суммы каждого
исходника. Архив содержит 35 файлов; проверены его целостность, отсутствие двух
исключённых исходников, совпадение содержимого и SHA-256 HTTP-скачивания.
Предыдущий запуск со всеми 32 исходниками сохранён.

Готовый комплект находится в
`data/runs/e67a66cc68a643e7a4eb8fc04be8d51d/`.
Копия Markdown сохранена в Obsidian:
`/Users/mustafa/Library/Mobile Documents/iCloud~md~obsidian/Documents/Заметки/Работа/Активация — 22.09.2026.md`.

## Проверки изменений

- `RUN_BROWSER_TESTS=1 .venv/bin/python -m pytest -q`: **146 passed, 54 subtests passed**.
- Проверены точное сопоставление ID, переименование, одинаковые имена разных ID,
  пропуск до сетевых запросов, строгая конфигурация и сохранение исключений в аудите.
- Приложение перезапущено с новой конфигурацией и обработчиками.
- В реальном браузере подтверждены «Готово», 30 исходных файлов, список двух
  временных исключений и ссылка на готовый ZIP.
