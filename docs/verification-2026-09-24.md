# Проверка Artel Operator после обновления — 24.09.2026

Проверен работающий checkout `/Users/mustafa/Desktop/artel-operator`; приложение доступно на `http://127.0.0.1:8790/`. Новых воспроизводимых дефектов в проверенных сценариях не обнаружено. Код и настройки операторов не менялись.

## Подтверждено

- LaunchAgent `ru.artel.operator`, один процесс PID 13516, localhost:8790, workers=1. После применения обновления нет ошибок startup/scheduler; активных queued/running нет.
- SQLite `PRAGMA integrity_check`: `ok`. История содержит прежние 34 запуска. Все **216** зарегистрированных файлов истории существуют и совпадают с сохранёнными SHA256.
- ZIP последних успешных запусков GloPro, Яндекс из кабинета и SEO скачаны через штатные HTTP URL: HTTP 200, SHA256 совпали, CRC без ошибок.
- В браузере проверены обзор, операторы, история, детали результата, редактор инструкций, подключения, форма импорта и форма ручного запуска. Периоды GloPro/недельных фирм/Яндекса отображаются правильно. Ошибок или предупреждений console не обнаружено. Изменения форм не отправлялись.
- Запросы с чужими Origin/Host отклоняются HTTP 403.
- Независимый полный прогон `RUN_BROWSER_TESTS=1 .venv/bin/python -m pytest -q`: **310 passed, 54 subtests passed**, 4.89 секунды. Два прежних deprecation warning Starlette/httpx и AnyIO. `git diff --check` проходит. Review runtime diff и 11 новых случаев не выявил новых воспроизводимых дефектов.
- **Свежий вход GloPro подтверждён** штатным `/api/connection/test`: HTTP 200, verified=true, полный список из 32 клиентов, 57.4 секунды. Отчёты не формировались. Настройка connection_verified подтверждена как true.
- SEO: пути Codex/Node/Python и файлы контракта publisher существуют; зависимости установлены; Codex CLI сообщает вход через ChatGPT. Свежий контекст из Strapi получен: 16 товаров, 7 статей, 6 документов, 19 путей.

## Расписание

Все три оператора включены. Следующие запуски в МСК: GloPro — **25.09 07:00**, SEO — **27.09 12:00**, Яндекс — **29.09 07:00**. Штатные настройки не менялись. Mac должен работать и не спать.

## Границы проверки

Яндекс: сохранённая сессия присутствует; последний успешный кабинетный запуск `d90704dd0b154b53aa1caf23c7785488` от 22.09 содержит 6 заказов, 2 сотрудников и activity_check_complete=true. Более поздние успешные импорты не доказывают полноту кабинета. Действительность сессии сегодня новым завершённым входом/выгрузкой не подтверждена.

Новый полный цикл SEO (AI, imagegen, SSH, публикация) и новые топливные выгрузки не запускались. Свежий контекст CMS и локальные тесты не доказывают возможность новой публикации. Внешние заметки Obsidian и статьи при проверках не изменялись. Два дополнительных кабинетных probe были остановлены до получения результата; их не учитываем как подтверждение.
